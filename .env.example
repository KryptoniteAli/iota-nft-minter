# Pinata JWT for direct browser uploads to IPFS
# Get this from https://app.pinata.cloud/developers/api-keys
VITE_PINATA_JWT=your_pinata_jwt_here

# --- Vercel serverless API (api/pinata-url.ts) ---
# These are set in Vercel project settings, not needed for local dev
# PINATA_JWT=your_pinata_jwt_here
# PINATA_GATEWAY=your_pinata_gateway_here
